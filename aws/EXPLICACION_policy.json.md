Comentarios añadidos en español: `policy.json` contiene la política IAM usada para desplegar recursos en AWS.
Cómo lo logra: define permisos y acciones (ARNs) que permiten operaciones específicas (S3, CloudFront, IAM) necesarias durante despliegues automatizados.
