Comentarios añadidos en español: `passrole.json` describe el permiso para que un servicio asuma un rol al ejecutar acciones.
Cómo lo logra: expone la política que permite el `PassRole` a servicios o entidades específicas para delegar permisos durante despliegues.
