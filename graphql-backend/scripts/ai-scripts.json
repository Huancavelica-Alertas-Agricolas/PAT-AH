{
  "name": "ai-microservice-scripts",
  "scripts": {
    "install-ai-deps": "npm install multer xlsx axios ml-regression-simple-linear ml-regression-multivariate-linear ml-matrix ml-stat @tensorflow/tfjs-node csv-parser @types/multer",
    "setup-ai": "mkdir -p uploads trained-models && echo 'Directorios creados para el microservicio de IA'",
    "test-weather-api": "node -e \"const axios = require('axios'); axios.get('https://api.openweathermap.org/data/2.5/weather?q=Lima&appid=demo&units=metric').then(r => console.log('API funcionando')).catch(e => console.log('Configurar OPENWEATHER_API_KEY'));\"",
    "cleanup-models": "rm -rf trained-models/* && echo 'Modelos eliminados'",
    "cleanup-uploads": "rm -rf uploads/* && echo 'Archivos subidos eliminados'",
    "ai-status": "curl -X GET http://localhost:3001/ai/health || echo 'Microservicio no disponible'"
  }
}
